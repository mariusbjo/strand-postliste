name: Auto-publish

on:
  workflow_run:
    workflows: ["Morgen-scraping", "Oppdatering-scraping", "Full historical scrape", "Publish-dates-scraping"]
    types:
      - completed
  workflow_dispatch:   # gir deg manuell trigger i Actions-fanen

permissions:
  contents: write

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - name: Sjekk ut repo
        uses: actions/checkout@v3
        with:
          ref: main   # sjekk ut main branch (Pages bygger fra denne)
          persist-credentials: true
          fetch-depth: 0

      - name: Sett opp Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.11"

      # Avhengigheter: kun det generate_html.py faktisk trenger.
      - name: Installer avhengigheter
        run: |
          python -m pip install --upgrade pip

      - name: Generer index.html fra template og data
        run: python generate_html.py

      - name: Sett opp git remote
        run: |
          git remote remove origin || true
          git remote add origin https://x-access-token:${{ secrets.GITHUB_TOKEN }}@github.com/${{ github.repository }}.git
          git remote -v

      - name: Commit og push frontend
        run: |
          git config --global user.name "${{ github.actor }}"
          git config --global user.email "${{ github.actor }}@users.noreply.github.com"
          git add index.html web/script.js web/style.css web/template.html postliste.json
          git commit -m "Automatisk publish etter scraping" || echo "Ingen endringer å committe"
          # Hent siste endringer fra main og legg dine commits oppå
          git pull --rebase origin main || echo "Ingen endringer å hente"
          git push origin main

